import Header from "../components/Header";

const Add = () => {
    return (
        <>
            <Header
                title={'지출/수입 등록'}
                leftChild={'버튼 자리'}
                rightChild={'등록, 저장 자리'}
            />
            <div>
                안녕
            </div>
        </>
    );
};

export default Add;